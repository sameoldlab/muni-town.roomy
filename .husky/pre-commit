# Runs before every commit, modify tasks in package.json
# Disabling for now because we have a lot to fix.
#pnpm lint-staged

# Test using "exit 1" to cancel commit before actually making it
# exit 1
