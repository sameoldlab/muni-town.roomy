/**
 * Stub for `$app/environment` — @foxui/core's sanitize.js imports
 * `browser` from it. Storybook renders client-side, so browser = true.
 */

export const browser = true;
export const dev = false;
export const building = false;
export const prerendering = false;
export const version = "storybook";
