/**
 * Stub for `$app/navigation` — the design system's only `$app/*` import is
 * `goto` in components/layout/ToggleTabs.svelte, and Storybook has no
 * SvelteKit runtime. The stub keeps ToggleTabs renderable; `goto` no-ops.
 */

export function goto(): void {
  /* no-op in Storybook */
}

export function invalidate(): void {
  /* no-op in Storybook */
}

export function invalidateAll(): void {
  /* no-op in Storybook */
}

export function prefetch(): void {
  /* no-op in Storybook */
}

export function prefetchRoutes(): void {
  /* no-op in Storybook */
}

export const afterNavigate = (): void => {
  /* no-op in Storybook */
};

export const beforeNavigate = (): void => {
  /* no-op in Storybook */
};

export const onNavigate = (): void => {
  /* no-op in Storybook */
};
